services:
  db:
    image: postgres:15-alpine
    container_name: hospital_db
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_DB=${DB_NAME:-hospital}
      - POSTGRES_USER=${DB_USER:-postgres}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-postgres}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-hospital}"]
      interval: 10s
      timeout: 5s
      retries: 5
    env_file:
      - .env

  web:
    build: .
    container_name: hospital_web
    restart: unless-stopped
    volumes:
      - static_volume:/app/Hospital_System/staticfiles
      - media_volume:/app/Hospital_System/media
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgres://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@db:5432/${DB_NAME:-hospital}
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "
        python manage.py migrate --noinput &&
        python manage.py collectstatic --noinput &&
        gunicorn --bind 0.0.0.0:8000 --workers 3 --timeout 120 Hospital_System.wsgi:application
      "

volumes:
  postgres_data:
    name: hospital_postgres_data
  static_volume:
    name: hospital_static_files
  media_volume:
    name: hospital_media_files
